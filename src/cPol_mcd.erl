%%%-------------------------------------------------------------------
%%% Created : 30. Apr 2020 8:10 น.
%%%-------------------------------------------------------------------
-module(cPol_mcd).

%% API
-export([generate/2]).

generate(mcd01, Ipd) ->
  io:format("Ipd*********: ~p~n", [Ipd]),
  ok;
generate(mcd02, Ipd) ->
  io:format("Ipd*********: ~p~n", [Ipd]),
  ok.
